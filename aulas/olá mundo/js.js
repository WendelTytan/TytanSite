var resultado = 2 * 5;

document.getElementById('resultado').value = resultado;

// Obtém a referência para o elemento com o ID "resultado"
var elementoResultado = document.getElementById("resultado1");

// Sua variável JavaScript
var meuValor = "Olá, mundo!";

// Define o conteúdo do elemento com o valor da variável
elementoResultado.innerHTML = meuValor;